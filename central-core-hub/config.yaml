name: "Central Core Hub"
description: "Central Core Hub Add-on"
version: "1.0.8"
slug: "central-core-hub"
init: false
arch:
  - aarch64
  - amd64
  - armhf
  - armv7
  - i386
startup: services
boot: auto
stage: stable
timeout: 30
ports:
  "8000/tcp": 8000
options:
  greeting: "Hello from Central Core Hub!"
schema:
  greeting: str
