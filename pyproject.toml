[tool.ruff]
line-length = 120
exclude = [".venv"]
# Keep the default rule set but allow projects to add ignores via CLI or
# CI as needed. This file centralizes ruff configuration for local and CI runs.
