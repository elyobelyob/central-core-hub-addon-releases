paho-mqtt==2.1.0
requests==2.34.2
pydantic==2.13.4
pytest==9.1.1
pytest-cov==7.1.0
websocket-client==1.9.0
PyYAML==6.0.3  # required by mqtt_client.py to parse SENSOR_REGISTRY.yaml

# Prefer the released PyPI package if available. Keep the VCS fallback
# for environments where the package hasn't been published yet.
central-core-mqtt-shared @ git+https://github.com/elyobelyob/central-core-mqtt-shared.git@v1.0.2
